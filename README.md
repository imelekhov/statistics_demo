# statistics_demo
Python implementation of programming assignments of Chapter 4 of the original book "Natural Image Statistics" by A. Hyvarinen.

## Installation ##
* Clone the repository
* Create and activate new virtualenv
* Install all dependencies with `pip install -r requirements.txt`

For running script, just type `jupyter notebook`
